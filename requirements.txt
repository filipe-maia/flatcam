# This file contains python only requirements to be installed with pip.
# Python pacakges that cannot be installed with pip are not included.
# Previous versions of FlatCAM required shapely>=1.3,<2. Current version of FlatCAM tested only with shapely=2.1.1.
# Usage: pip install -r requirements.txt
# P.S.: Are 'sip' and 'filefinder' also required?
#sip
pyqt5
shapely==2.1.1
numpy>=1.8
matplotlib>=1.3.1
simplejson
rtree
scipy
svg.path
#filefinder
